version: '2'
services:

  web_python:
    environment:
      - PYTHONUNBUFFERED=0
    build: ./python_app
    ports:
      - "5000:5000"

  web_nginx:
    build: ./nginx_app
    ports:
      - "80:80"

  web_node:
     build: ./node_app
     ports:
       - "8000:8000"
