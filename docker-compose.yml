# base/docker-compose.yml
# Use this file to set up your containers.
#
# Author: Nikhil Kansal
# Version: 1.0.0
# Date: 06/26/2017

version: '2'
services:

  web_python:
    build: ./python_app
    ports:
      - "5000:5000"
    volumes:
      - ./python_app:/app

  web_nginx:
    build: ./nginx_app
    ports:
      - "3000:3000"
    volumes:
      - ./nginx_app:/app
