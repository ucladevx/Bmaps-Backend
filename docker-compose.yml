version: '2'
services:

  web_python:
    build: ./python_app
    ports:
      - "5000:5000"

  web_nginx:
    build: ./nginx_app
    ports:
      - "80:80"

  fb_events:
    image: tobilg/facebook-event-search
    ports:
      - "3000:3000"
